Q1 -> What is JPA?
A  -> Java Persistence API
	Standard API for ORM

	Only a specification
		Defines a set of interfaces
		Requires an implementation to be usable
		
Q2 -> WHat are some JPA Vendor Implementations?
A  -> Hibernate			and 		EclipseLink

Q3 -> JDBC?
A  -> It had Connection, prepared Statements, and then mapping to class.

Q4 -> Spring JDBC?
A  -> It made mapping easy, by providing a layer on top of JDBC, used concept like JDBCTemplate, you still have to write query, we can implement RowMappers which has a resultset

Q5 -> Entity manager?
A  -> it is an interface to the Persistence Context, it only tracks those objects which are persistent through it

Q6 -> @PersistenceContext?
A  -> 
		

	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	